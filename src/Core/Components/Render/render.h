#ifndef CORE_COMPONENTS_RENDER
#define CORE_COMPONENTS_RENDER

namespace Engine
{
// currently only signals if component is supposed to be rendered
class RenderComponent
{
public:
    RenderComponent();
};

} // namespace Engine

#endif